<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Scrypto – Premium Mobile Tile</title>
  <style>
    :root {
      --bg: #0f1221;
      --surface: #111428;
      --text: #0a0a0a;
      --text-on-dark: #eef2ff;
      --muted: #a5b0c4;
      --ring: 0 0 0 3px color-mix(in lab, var(--accent) 35%, transparent);
      --radius: 18px;
    }

    /* Demo page frame (mobile viewport) */
    body {
      margin: 0; display: grid; place-items: center; min-height: 100svh;
      background: radial-gradient(1200px 600px at 10% 10%, #1a1f3a 0%, transparent 60%),
                  radial-gradient(1000px 800px at 100% 0%, #122034 0%, transparent 50%),
                  #0b0e1a;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Inter, "Helvetica Neue", Arial;
    }

    .phone {
      width: 390px; max-width: 92vw; background: #f7f8fb; border-radius: 26px; overflow: hidden;
      box-shadow: 0 12px 40px rgba(0,0,0,.45), inset 0 1px 0 rgba(255,255,255,.5);
      border: 1px solid rgba(255,255,255,.6);
    }
    .appbar { background: #2f5af3; color: white; padding: 18px 22px; font-weight: 700; }
    .wrap { padding: 20px; background: linear-gradient(180deg,#fbfcff, #f3f6ff 280px, #f7f8fb); }

    /* --- Tile component --- */
    .tile {
      --accent: #2f5af3; /* default */
      --accent-soft: color-mix(in lab, var(--accent) 12%, white);
      --accent-bg: color-mix(in lab, var(--accent) 6%, white);

      position: relative; width: 100%; border: 1px solid color-mix(in lab, var(--accent) 22%, #e9eef5);
      background:
        radial-gradient(120% 120% at 0% 0%, color-mix(in lab, var(--accent) 14%, white), transparent 60%),
        linear-gradient(180deg, white, var(--accent-bg));
      border-radius: var(--radius); padding: 14px 16px; text-align: left; cursor: pointer; outline: none;
      display: grid; grid-template-columns: 52px 1fr auto; gap: 12px; align-items: center;
      box-shadow: 0 1px 0 rgba(255,255,255,.9), 0 10px 30px rgba(15, 35, 80, .12);
      transition: transform .12s ease, box-shadow .12s ease, border-color .12s ease;
    }
    .tile + .tile { margin-top: 14px; }

    .tile:hover { transform: translateY(-1px); box-shadow: 0 1px 0 rgba(255,255,255,.9), 0 16px 38px rgba(15,35,80,.18); }
    .tile:active { transform: translateY(0); box-shadow: 0 1px 0 rgba(255,255,255,.9), 0 8px 18px rgba(15,35,80,.18); }
    .tile:focus-visible { box-shadow: var(--ring), 0 16px 38px rgba(15,35,80,.2); }

    .tile::after { /* subtle top highlight */
      content: ""; position: absolute; inset: 0; border-radius: inherit; pointer-events: none;
      background: linear-gradient(180deg, rgba(255,255,255,.8), transparent 24%);
      mix-blend-mode: soft-light; opacity: .65;
    }

    .tile .icon {
      width: 52px; height: 52px; border-radius: 14px; display: grid; place-items: center;
      background: conic-gradient(from 210deg, color-mix(in lab, var(--accent) 18%, #fff), white);
      border: 1px solid color-mix(in lab, var(--accent) 30%, #dfe8ff);
      box-shadow: inset 0 1px 0 white, 0 8px 18px rgba(47,90,243,.10);
    }
    .tile .icon svg { width: 26px; height: 26px; stroke: var(--accent); }

    .tile .content h3 { margin: 0; font-size: 18px; line-height: 1.25; color: #0b1324; }
    .tile .content p { margin: 6px 0 0 0; font-size: 13px; color: #62708d; }

    .tile .meta { display:flex; align-items: center; gap: 8px; }
    .meta .badge { font-size: 11px; padding: 4px 8px; border-radius: 999px; background: var(--accent-soft); color: #203050; border:1px solid color-mix(in lab, var(--accent) 30%, #e7eefc); }
    .meta .chev { width: 22px; height: 22px; border-radius: 999px; display:grid; place-items:center; background: #f0f4ff; border:1px solid #e0e7ff; }
    .meta .chev svg { width: 14px; height:14px; stroke:#345; }

    /* Variants */
    .tile.success { --accent:#22c55e; }
    .tile.warning { --accent:#f59e0b; }
    .tile.info    { --accent:#0ea5e9; }

    /* Compact modifier */
    .tile.compact { grid-template-columns: 44px 1fr auto; padding:12px 14px; }
    .tile.compact .icon { width:44px; height:44px; border-radius:12px; }

    /* Dark-mode preview toggle (optional) */
    .toolbar { display:flex; gap:10px; align-items:center; margin-bottom:12px; }
    .toggle { appearance:none; width:42px; height:26px; border-radius:999px; background:#dde3f6; position:relative; outline:none; cursor:pointer; border:1px solid #c9d3ef; }
    .toggle:checked { background:#1e293b; border-color:#111827; }
    .toggle::after { content:""; position:absolute; top:3px; left:3px; width:20px; height:20px; border-radius:50%; background:white; box-shadow:0 1px 2px rgba(0,0,0,.2); transition:left .15s ease; }
    .toggle:checked::after { left:19px; }

    .dark .phone { background: #0f1529; border-color: rgba(255,255,255,.08); }
    .dark .appbar { background:#1f2e88; }
    .dark .wrap { background: linear-gradient(180deg,#0f1529,#0b1120 280px,#0f1424); }
    .dark .tile { background:
        radial-gradient(120% 120% at 0% 0%, color-mix(in lab, var(--accent) 14%, #0b1220), #0b1324 60%),
        linear-gradient(180deg, color-mix(in lab, #0f162b 85%, var(--accent) 5%), #0b1221);
      color:#eaf1ff; border-color: color-mix(in lab, var(--accent) 30%, #1f2a46);
    }
    .dark .tile .content h3 { color:#eef3ff; }
    .dark .tile .content p { color:#9db0d4; }
    .dark .tile .icon { background: linear-gradient(180deg, color-mix(in lab, var(--accent) 18%, #0f162b), #0b1221);
      border-color: color-mix(in lab, var(--accent) 45%, #1f2a46);
    }
    .dark .meta .badge { color:#cfe2ff; background: color-mix(in lab, var(--accent) 20%, #0f162b); border-color: color-mix(in lab, var(--accent) 50%, #1e293b); }
    .dark .meta .chev { background:#111a2a; border-color:#1b2a40; }
  </style>
</head>
<body>
  <div class="phone" id="phone">
    <div class="appbar">Scrypto</div>
    <div class="wrap">
      <div class="toolbar">
        <label style="font-size:12px;color:#5b6a8a">Dark Mode</label>
        <input id="dm" class="toggle" type="checkbox" />
      </div>

      <!-- Tile examples -->
      <button class="tile" style="--accent:#3b82f6" aria-label="Medical History">
        <div class="icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M3 12h4l2 6 4-12 2 6h4"/>
          </svg>
        </div>
        <div class="content">
          <h3>Medical History</h3>
          <p>Allergies, conditions, immunizations</p>
        </div>
        <div class="meta">
          <span class="badge">Updated</span>
          <span class="chev" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 18l6-6-6-6"/>
            </svg>
          </span>
        </div>
      </button>

      <button class="tile success" aria-label="Medications">
        <div class="icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M9 12h6"/><path d="M12 9v6"/>
          </svg>
        </div>
        <div class="content">
          <h3>Medications</h3>
          <p>Active prescriptions & adherence</p>
        </div>
        <div class="meta">
          <span class="badge">3 active</span>
          <span class="chev" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 18l6-6-6-6"/>
            </svg>
          </span>
        </div>
      </button>

      <button class="tile warning compact" aria-label="Appointments">
        <div class="icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="4" width="18" height="18" rx="3"/>
            <path d="M16 2v4M8 2v4M3 10h18"/>
          </svg>
        </div>
        <div class="content">
          <h3>Appointments</h3>
          <p>Schedule & manage visits</p>
        </div>
        <div class="meta">
          <span class="badge">Today 14:30</span>
          <span class="chev" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 18l6-6-6-6"/>
            </svg>
          </span>
        </div>
      </button>

    </div>
  </div>

  <script>
    const dm = document.getElementById('dm');
    const phone = document.getElementById('phone');
    dm.addEventListener('change', () => {
      phone.classList.toggle('dark', dm.checked);
    });
  </script>
</body>
</html>
